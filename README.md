# Synonym Service (FastAPI)

Implements the "Data Engine Synonym System" with both Redis and in‑memory cache backends, a
repository abstraction for SQL Server, structured logging, retries, health checks, and graceful shutdown.
